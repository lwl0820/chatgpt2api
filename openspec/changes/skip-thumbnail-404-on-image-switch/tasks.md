## 1. 缩略图状态建模

- [x] 1.1 为选图候选补充缩略图可用性标记，支持 `unknown / available / missing` 或等价状态
- [x] 1.2 确认该状态随选图会话恢复一起读写，确保页面切换后还能复用
- [x] 1.3 在候选身份、资源路径或重新生成时清理旧状态，避免缓存串用

## 2. 候选加载逻辑优化

- [x] 2.1 在解析候选图片资源前先检查缩略图状态，命中 `missing` 时直接返回原图地址
- [x] 2.2 在首次回退到原图的路径上写回 `missing` 状态，避免后续再次请求缩略图
- [x] 2.3 保持已有缩略图优先展示、原图预加载和 immersive 逻辑不变

## 3. 验证与回归

- [x] 3.1 补充自动化测试，覆盖“首次缺失触发回退”和“再次切换不再请求缩略图”两个场景
- [x] 3.2 验证存在缩略图的候选仍然优先使用缩略图，不影响当前选图体验
- [x] 3.3 手工检查网络请求，确认同一候选在已知缺失后不再产生重复 404
