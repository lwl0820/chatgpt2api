## 1. 后端图片链路

- [x] 1.1 新增 `image_thumbnail_generation` 配置项，并补齐默认开启的归一化与持久化逻辑。
- [x] 1.2 从生成结果保存路径中移除保留期清理，避免一次成功写图就触发递归目录扫描。
- [x] 1.3 只让缩略图创建受新设置控制，缩略图清理和缩略图响应保持现有行为。

## 2. 设置入口

- [x] 2.1 扩展 settings API/store 的类型和读写逻辑，让新的缩略图开关能够正常加载和保存。
- [x] 2.2 在 `ConfigCard` 中新增管理员缩略图开关，文案风格与现有图片设置区一致。

## 3. 客户端兼容性

- [x] 3.1 确认图片管理、日志和图片选图里的缩略图消费路径已有原图回退能力，必要时只补齐前端回退逻辑。

## 4. 测试与验证

- [x] 4.1 为保存路径不再触发清理、缩略图开关只控制创建补充或更新后端测试。
- [x] 4.2 为默认开启的缩略图生成和新配置项的持久化补充或更新 settings 测试。
- [x] 4.3 运行相关的图片与 settings 测试集，并修复发现的回归。
