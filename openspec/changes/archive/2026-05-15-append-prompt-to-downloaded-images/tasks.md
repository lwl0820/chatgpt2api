## 1. 共享提示词尾部载荷

- [x] 1.1 新增默认关闭的持久化配置项，并让设置 API 读取和保存该布尔选项
- [x] 1.2 在设置页面新增“下载图片时追加提示词”开关，默认显示为关闭
- [x] 1.3 新增共享工具函数，在开关启用且提示词非空时按统一 UTF-8 边界格式追加到 Blob 或二进制字节末尾
- [x] 1.4 新增共享下载函数，支持从 Blob、data URL、base64 或远程 URL 构造下载文件，并保持既有文件名规则
- [x] 1.5 为共享工具和设置默认值补充单元测试，覆盖开关关闭不改写、开关开启有提示词追加、无提示词不改写、文件名不包含提示词

## 2. 前端图片下载入口

- [x] 2.1 更新画图会话结果下载，在设置启用时将所属 turn 的 prompt 传入共享下载函数
- [x] 2.2 更新图片预览弹窗数据结构和下载逻辑，使预览下载在设置启用时能携带当前图片关联的提示词
- [x] 2.3 更新图片选择页面下载逻辑，在设置启用时将 selection session prompt 追加到候选图下载文件数据末尾
- [x] 2.4 验证图片选择下载后仍按现有语义标记候选图为 kept、记录 undo 历史并前进到下一张

## 3. 图片管理器下载接口

- [x] 3.1 梳理现有会话和图片选择 session 数据，建立可复用的图片相对路径到提示词的查询逻辑
- [x] 3.2 更新单图下载接口，在设置启用且能确定提示词时返回尾部追加提示词载荷的响应，否则保持原文件响应
- [x] 3.3 更新批量 zip 下载逻辑，在设置启用且能确定提示词时只改写 zip 条目内文件数据，保持条目命名和去重规则不变
- [x] 3.4 为图片管理器下载补充后端测试，覆盖默认关闭、单图追加、zip 条目追加、无提示词保持原始内容

## 4. 验证与回归

- [x] 4.1 运行前端相关测试，确认画图结果、预览弹窗和图片选择下载入口通过
- [x] 4.2 运行后端相关测试，确认图片管理器下载接口通过
- [x] 4.3 手动检查关闭开关时下载内容不追加提示词
- [x] 4.4 手动启用开关并检查一个下载文件的末尾内容，确认提示词写入文件数据末尾而不是文件名
