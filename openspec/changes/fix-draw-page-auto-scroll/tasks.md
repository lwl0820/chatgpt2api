## 1. 滚动行为实现

- [x] 1.1 在 `web/src/app/image/page.tsx` 中移除基于任意 `selectedConversation` 引用和单纯 `updatedAt` 变化的自动滚动。
- [x] 1.2 为用户提交新画图请求、重试、重新生成和继续编辑等入口增加一次性“滚到底部”意图。
- [x] 1.3 调整结果视口 effect，仅在消费到用户发起的新内容滚动意图时强制滚动到底部。
- [x] 1.4 确保任务轮询、任务完成和任务失败状态写入期间不会触发自动滚动。

## 2. 回到底部按钮

- [x] 2.1 为结果视口增加滚动位置监听，维护“回到底部”悬浮按钮的显示状态。
- [x] 2.2 在画图结果区域添加“回到底部”悬浮按钮，点击后滚动到底部。
- [x] 2.3 确保按钮在用户位于底部时隐藏或不可用，并且不影响移动端和桌面端布局。

## 3. 场景验证

- [x] 3.1 验证用户滚到上方查看历史内容时，处理中任务每隔数秒更新不会把结果视口拉到底部。
- [x] 3.2 验证任务成功或失败时不会自动改变用户当前滚动位置。
- [x] 3.3 验证提交新画图请求、重试、重新生成或继续编辑时，新轮次或新任务会出现在用户可见区域。
- [x] 3.4 验证用户点击“回到底部”按钮后，结果视口滚动到底部。

## 4. 回归检查

- [x] 4.1 确认图片任务创建、轮询、成功/失败状态转换和会话持久化行为未被滚动策略改变。
- [x] 4.2 运行项目可用的前端检查命令，至少覆盖 TypeScript、lint 或现有测试中的相关一项。
