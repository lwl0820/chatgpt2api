## 1. 会话状态与统计

- [x] 1.1 扩展选图会话模型，增加可持久化的统计重置元数据，并为历史会话提供安全的默认归一化值。
- [x] 1.2 调整候选决策和失败路径，确保保留、丢弃、跳过统计都能按最近一次重置点正确归因。
- [x] 1.3 更新共享的会话统计工具，让它能返回感知重置后的保留、丢弃、跳过计数，同时保留活动队列计数。

## 2. 重置入口界面

- [x] 2.1 为当前选中的会话增加“重置统计”处理逻辑，重置需要持久化，但不能改变会话排序用的 `updatedAt`。
- [x] 2.2 在会话控制区增加可见的“重置统计”按钮，并处理不可用状态和空会话状态。
- [x] 2.3 确保普通查看、沉浸查看和会话列表在重置后展示一致的统计值。

## 3. 验证

- [x] 3.1 补充或更新重置感知统计的测试，包括重置持久化以及重置后新增活动继续计数。
- [x] 3.2 验证重置不会改变候选状态、图片文件、队列状态、会话配置或会话列表排序。
- [ ] 3.3 运行相关的前端测试、lint 或类型检查命令，并修复发现的失败。
